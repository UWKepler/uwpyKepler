 uwpyKepler used to access the local public Kepler lightcurves 
 
 Authors - E. Agol, A. C. Becker, N. H. Thomas, C. J. Martin, 
           T. Steckler, J. Mehlhaff & P. Kundurthy 
 
 Institution - University of Washington, Seattle 
 Last Updated: 2011-Oct-21 01:09:37 PM


+ lcmod.py
     ApplyMask(lcdtData,mask):
     ApplyMaskPortions(lcData,mask,portion):
     returnData(lcdtData,typeTag):

+ detectfuncs.py

+ pipelinepars.py
     readPipelinePars(File):
     SortParOptions(OptionString):
     computeNTrials(ParObject):
     class PipelinePars:
     TrialList(UberList):
     class keptrial:

+ dbinfo.py
     getKIDsFP():
     getKIDsPC():
     getKIDsSource():
     returnSkyGroupKIDs(SGNumber):
     returnCoordKID(KIDlist):
     returnKIDsInKEPFP(KIDlist):
     returnKIDsInKEPPC(KIDlist):
     returnKIDsNonKOI(KIDlist):
     returnRstar(KID):
     returnLOGG(KID):

+ debug.py
     getUniqueTraceback(logFile):

+ func.py
     getNumBool(List,bool):
     makeDTwindows(nsize,window):
     makeRangePairs(indexList):
     movingMedian(data,window):
     compute1Sigma(data):
     bin2(data,binlist):

+ iofiles.py
     readKIDsFromFile(File):

+ qatspars.py
     getListIndicies(Array,ListValues):
     stellar_dens(KID):
     tdur(rho_s,b,period):
     period_snr(data,period0,f,nperiod,ds):
     class qatslc:

+ pipeline.py
     FlagKeplerEvents(lcData, **kwargs):
     RemoveBadEvents(lcData):
     FlagEclipses(lcData,eclipseData,BJDREFI, **kwargs):
     SplitPortions(lcData,gapsize):
     FlagOutliers(lcData,medwin,threshold):
     DetrendData(lcData, window, polyorder):
     StackPortions(lcData):

+ keplc.py
     class keplc:
     class lcData:
     class kw:

+ iodb.py
     dbConnect(dBhost,dBuser,dBpass,dBname):
     inSource(KeplerID):
     inKEPPC(KeplerID):
     inKEPFP(KeplerID):
     getKOI(KeplerID):
     ReadLightCurve(KeplerID, **kwargs):
     getEclipseData(KeplerID):
     getBJDREFI(KeplerID):

+ qats.py
     gamma_m(m,tmin,tmax,n,q):
     omega_m(m,mm,tmin,tmax,n,q):
     fmax(MM,N,tmin,tmax,q,dc):
     qpt_convolve(data,q):
     qpt_detect(data,tmin,tmax,q):

